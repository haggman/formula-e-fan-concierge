"""Commentator agent (ADK) — STARTER. Build this across Tiers A–D.

Tier A: define root_agent with a broadcast prompt (no tools) — watch it invent.
Tier B: add the field-wide frame tools (tools/frame_tools.py).
Tier C: make it selection-aware (selected_car).
Tier D: wire TTS.

See solution/commentator/ for the reference and spec/frame_tools_scorer_reaim.md.
"""
# TODO(student): root_agent = Agent(...)
