"""Field-wide live-state tools — STARTER (Tier B/C).
Build get_field_state(selected_car=None) + port get_recent_events /
get_events_in_range / get_field_am_status from Ch2. See the spec."""
# TODO(student)
