# Race-Data Subagent — STARTER
Build the ADK agent that owns Firestore "now" + BigQuery "then" (R10 + career),
time-honest, then wrap it as an MCP server for CX. Reference:
solution/race_data_subagent/. Spec: spec/cx_integration_spike.md.
