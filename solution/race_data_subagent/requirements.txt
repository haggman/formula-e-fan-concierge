# Cloud Run service deps for the race-data subagent (the agent IS the service).

# Web service / OpenAPI surface
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# Live "now" plane (Firestore reader in shared/)
google-cloud-firestore>=2.16.0

# ADK agent (LLM mode) + ToolboxToolset (BigQuery via fe-toolbox) + A2A door.
# IMPORTANT: ToolboxToolset requires the [toolbox] extra (pulls toolbox-adk) —
# [a2a] alone is NOT enough (you'll get "ToolboxToolset requires 'toolbox-adk'").
# [a2a] pulls the A2A extras for to_a2a(); toolbox-core is the underlying client.
google-adk[a2a,toolbox]>=1.29.0
toolbox-core>=0.1.0
google-cloud-aiplatform>=1.144.0
